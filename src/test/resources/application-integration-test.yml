management:
  endpoints:
    web:
      base-path: /
      exposure:
        include: health
  endpoint:
    health:
      show-details: never
  metrics:
    enabled: false

aws:
  sqs:
    notification:
      # Placeholder so @NotBlank validation passes for ITs that don't use the SQS listener.
      # LocalStack default account ID (000000000000) and port (4566) satisfy validation here;
      # NotificationSqsListenerIT overrides this via @DynamicPropertySource before the listener starts.
      queue-url: http://localhost:4566/000000000000/trade_imports_animals_eu_notifications_gateway.fifo

azure:
  servicebus:
    transport-type: amqp

cdp:
  service-version: 0.0.0-test

logging:
  level:
    root: INFO
    uk.gov.defra.cdp.dynamicsgateway: INFO
    org.springframework: INFO

info:
  app:
    name: ${spring.application.name}
    version: ${cdp.service-version}
    environment: local
